{
  "name": "@plugins/spec-kit-cli",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun run typecheck && bun build src/cli.ts --outdir dist --target node",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
